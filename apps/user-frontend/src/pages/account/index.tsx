import { Text } from "@tarojs/components";
import { PageShell } from "@/modules/common/page-shell";

export default function AccountPage() {
  return (
    <PageShell title="账户与隐私" subtitle="删除链路与审计链路预留">
      <Text>待接入素材删除、任务删除、账户删除申请接口。</Text>
    </PageShell>
  );
}
