{
  "name": "@apps/webhook-dispatcher",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test:smoke": "tsx src/smoke.ts",
    "test:int007-local": "tsx src/int007-local.ts",
    "lint": "echo 'lint webhook-dispatcher: pending eslint setup'"
  },
  "dependencies": {
    "@packages/shared": "workspace:*",
    "@prisma/client": "^5.22.0"
  }
}
