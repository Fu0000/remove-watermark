generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Task {
  taskId       String   @id @map("task_id") @db.VarChar(64)
  userId       String   @map("user_id") @db.VarChar(64)
  assetId      String   @map("asset_id") @db.VarChar(64)
  mediaType    String   @map("media_type") @db.VarChar(16)
  taskPolicy   String   @map("task_policy") @db.VarChar(16)
  status       String   @db.VarChar(24)
  progress     Int
  version      Int
  errorCode    String?  @map("error_code") @db.VarChar(16)
  errorMessage String?  @map("error_message")
  resultUrl    String?  @map("result_url")
  createdAt    DateTime @map("created_at")
  updatedAt    DateTime @map("updated_at")

  mask TaskMask?

  @@index([userId, createdAt(sort: Desc)])
  @@map("tasks")
}

model TaskMask {
  taskId       String   @id @map("task_id") @db.VarChar(64)
  maskId       String   @map("mask_id") @db.VarChar(64)
  version      Int
  polygons     Json
  brushStrokes Json     @map("brush_strokes")
  updatedAt    DateTime @map("updated_at")

  task Task @relation(fields: [taskId], references: [taskId], onDelete: Cascade)

  @@map("task_masks")
}

model TaskIdempotencyKey {
  id             String   @id @db.VarChar(64)
  userId         String   @map("user_id") @db.VarChar(64)
  idempotencyKey String   @map("idempotency_key") @db.VarChar(128)
  payloadHash    String   @map("payload_hash")
  taskId         String   @map("task_id") @db.VarChar(64)
  createdAt      DateTime @map("created_at")
  updatedAt      DateTime @map("updated_at")

  @@unique([userId, idempotencyKey])
  @@index([taskId])
  @@map("idempotency_keys")
}

model TaskActionIdempotency {
  id             String   @id @db.VarChar(64)
  userId         String   @map("user_id") @db.VarChar(64)
  idempotencyKey String   @map("idempotency_key") @db.VarChar(128)
  payloadHash    String   @map("payload_hash")
  resultJson     Json     @map("result_json")
  updatedAt      DateTime @map("updated_at")

  @@unique([userId, idempotencyKey])
  @@map("task_action_idempotency")
}

model UsageLedger {
  ledgerId    String   @id @map("ledger_id") @db.VarChar(64)
  userId      String   @map("user_id") @db.VarChar(64)
  taskId      String   @map("task_id") @db.VarChar(64)
  status      String   @db.VarChar(16)
  source      String   @db.VarChar(64)
  consumeUnit Int      @map("consume_unit")
  consumeAt   DateTime @map("consume_at")

  @@index([userId, consumeAt(sort: Desc)])
  @@index([taskId])
  @@unique([userId, taskId, status, source], map: "uk_usage_ledger_task_status_source")
  @@map("usage_ledger")
}

model OutboxEvent {
  eventId       String   @id @map("event_id") @db.VarChar(64)
  eventType     String   @map("event_type") @db.VarChar(64)
  aggregateType String   @map("aggregate_type") @db.VarChar(32)
  aggregateId   String   @map("aggregate_id") @db.VarChar(64)
  status        String   @db.VarChar(16)
  retryCount    Int      @map("retry_count")
  createdAt     DateTime @map("created_at")

  @@index([status, createdAt(sort: Asc)])
  @@index([aggregateType, aggregateId])
  @@map("outbox_events")
}

model Plan {
  planId       String   @id @map("plan_id") @db.VarChar(64)
  name         String   @unique(map: "uk_plans_name") @db.VarChar(64)
  price        Int
  currency     String   @default("CNY") @db.Char(3)
  monthlyQuota Int      @map("monthly_quota")
  features     Json     @map("features_json")
  sortOrder    Int      @default(0) @map("sort_order")
  isActive     Boolean  @default(true) @map("is_active")
  createdAt    DateTime @default(now()) @map("created_at")
  updatedAt    DateTime @default(now()) @map("updated_at")

  @@index([isActive, sortOrder(sort: Asc)], map: "idx_plans_active_sort")
  @@map("plans")
}

model Subscription {
  subscriptionId String    @id @map("subscription_id") @db.VarChar(64)
  userId         String    @map("user_id") @db.VarChar(64)
  planId         String    @map("plan_id") @db.VarChar(64)
  status         String    @db.VarChar(16)
  channel        String    @db.VarChar(16)
  externalOrderId String?  @unique(map: "uk_subscriptions_external_order_id") @map("external_order_id") @db.VarChar(128)
  startedAt      DateTime? @map("started_at")
  effectiveAt    DateTime? @map("effective_at")
  expireAt       DateTime? @map("expire_at")
  canceledAt     DateTime? @map("canceled_at")
  autoRenew      Boolean   @default(false) @map("auto_renew")
  metaJson       Json?     @map("meta_json")
  createdAt      DateTime  @default(now()) @map("created_at")
  updatedAt      DateTime  @default(now()) @map("updated_at")

  @@index([userId, createdAt(sort: Desc)], map: "idx_subscriptions_user_created_at")
  @@map("subscriptions")
}

model BillingReconcileMonthly {
  monthKey       String    @map("month_key") @db.Char(7)
  userId         String    @map("user_id") @db.VarChar(64)
  committedUnits Int       @default(0) @map("committed_units")
  heldUnits      Int       @default(0) @map("held_units")
  releasedUnits  Int       @default(0) @map("released_units")
  ledgerCount    Int       @default(0) @map("ledger_count")
  firstConsumeAt DateTime? @map("first_consume_at")
  lastConsumeAt  DateTime? @map("last_consume_at")
  sourceUpdatedAt DateTime @default(now()) @map("source_updated_at")
  reconciledAt   DateTime  @default(now()) @map("reconciled_at")
  createdAt      DateTime  @default(now()) @map("created_at")
  updatedAt      DateTime  @default(now()) @map("updated_at")

  @@id([monthKey, userId])
  @@index([updatedAt(sort: Desc)], map: "idx_billing_reconcile_monthly_updated_at")
  @@map("billing_reconcile_monthly")
}

model BillingReconcileCheckpoint {
  checkpointKey String    @id @map("checkpoint_key") @db.VarChar(64)
  watermarkAt   DateTime? @map("watermark_at")
  lastRunId     String?   @map("last_run_id") @db.VarChar(64)
  lastMode      String?   @map("last_mode") @db.VarChar(32)
  metaJson      Json?     @map("meta_json")
  createdAt     DateTime  @default(now()) @map("created_at")
  updatedAt     DateTime  @default(now()) @map("updated_at")

  @@map("billing_reconcile_checkpoints")
}

model BillingReconcileRun {
  runId         String    @id @map("run_id") @db.VarChar(64)
  mode          String    @db.VarChar(32)
  status        String    @db.VarChar(16)
  windowStart   DateTime? @map("window_start")
  windowEnd     DateTime? @map("window_end")
  scannedRows   Int       @default(0) @map("scanned_rows")
  impactedUsers Int       @default(0) @map("impacted_users")
  impactedMonths Int      @default(0) @map("impacted_months")
  mismatchCount Int       @default(0) @map("mismatch_count")
  summaryJson   Json?     @map("summary_json")
  errorMessage  String?   @map("error_message")
  startedAt     DateTime  @default(now()) @map("started_at")
  finishedAt    DateTime? @map("finished_at")

  @@index([startedAt(sort: Desc)], map: "idx_billing_reconcile_runs_started_at")
  @@index([status, startedAt(sort: Desc)], map: "idx_billing_reconcile_runs_status_started")
  @@map("billing_reconcile_runs")
}

model WebhookEndpoint {
  endpointId   String    @id @map("endpoint_id") @db.VarChar(64)
  userId       String    @map("user_id") @db.VarChar(64)
  name         String    @db.VarChar(128)
  url          String
  status       String    @db.VarChar(16)
  eventsJson   Json      @map("events_json")
  timeoutMs    Int       @map("timeout_ms")
  maxRetries   Int       @map("max_retries")
  activeKeyId  String    @map("active_key_id") @db.VarChar(32)
  secretJson   Json      @map("secret_json")
  createdAt    DateTime  @default(now()) @map("created_at")
  updatedAt    DateTime  @default(now()) @map("updated_at")
  deletedAt    DateTime? @map("deleted_at")
  deliveries   WebhookDelivery[]

  @@index([userId, status, createdAt(sort: Desc)], map: "idx_webhook_endpoints_user_status_created")
  @@index([status, updatedAt(sort: Desc)], map: "idx_webhook_endpoints_status_updated")
  @@map("webhook_endpoints")
}

model WebhookDelivery {
  deliveryId          String          @id @map("delivery_id") @db.VarChar(64)
  eventId             String          @map("event_id") @db.VarChar(64)
  userId              String          @map("user_id") @db.VarChar(64)
  endpointId          String          @map("endpoint_id") @db.VarChar(64)
  eventType           String          @map("event_type") @db.VarChar(64)
  status              String          @db.VarChar(16)
  attempt             Int
  requestHeaders      Json            @map("request_headers")
  payloadSha256       String          @map("payload_sha256") @db.VarChar(64)
  signatureValidated  Boolean         @map("signature_validated")
  failureCode         String?         @map("failure_code") @db.VarChar(64)
  errorMessage        String?         @map("error_message")
  responseStatus      Int?            @map("response_status")
  createdAt           DateTime        @default(now()) @map("created_at")
  updatedAt           DateTime        @default(now()) @map("updated_at")
  endpoint            WebhookEndpoint @relation(fields: [endpointId], references: [endpointId], onDelete: Cascade)

  @@index([endpointId, status, createdAt(sort: Desc)], map: "idx_webhook_deliveries_endpoint_status_created")
  @@index([eventId, attempt], map: "idx_webhook_deliveries_event_attempt")
  @@index([status, createdAt(sort: Desc)], map: "idx_webhook_deliveries_status_created")
  @@unique([endpointId, eventId, attempt], map: "uk_webhook_endpoint_event_attempt")
  @@map("webhook_deliveries")
}

model Asset {
  assetId       String    @id @map("asset_id") @db.VarChar(64)
  userId        String    @map("user_id") @db.VarChar(64)
  fileName      String    @map("file_name")
  fileSize      Int       @map("file_size")
  mediaType     String    @map("media_type") @db.VarChar(16)
  mimeType      String    @map("mime_type") @db.VarChar(128)
  sha256        String?   @db.VarChar(64)
  status        String    @db.VarChar(16)
  cleanupStatus String    @map("cleanup_status") @db.VarChar(16)
  createdAt     DateTime  @default(now()) @map("created_at")
  updatedAt     DateTime  @default(now()) @map("updated_at")
  deletedAt     DateTime? @map("deleted_at")

  @@index([userId, status, createdAt(sort: Desc)], map: "idx_assets_user_status_created")
  @@index([status, updatedAt(sort: Desc)], map: "idx_assets_status_updated")
  @@map("assets")
}

model TaskViewDeletion {
  deletionId String   @id @map("deletion_id") @db.VarChar(64)
  userId     String   @map("user_id") @db.VarChar(64)
  taskId     String   @map("task_id") @db.VarChar(64)
  deletedAt  DateTime @default(now()) @map("deleted_at")

  @@unique([userId, taskId])
  @@index([userId, deletedAt(sort: Desc)], map: "idx_task_view_deletions_user_deleted")
  @@map("task_view_deletions")
}

model AccountDeleteRequest {
  requestId String   @id @map("request_id") @db.VarChar(64)
  userId    String   @map("user_id") @db.VarChar(64)
  reason    String
  status    String   @db.VarChar(16)
  etaAt     DateTime @map("eta_at")
  startedAt DateTime? @map("started_at")
  finishedAt DateTime? @map("finished_at")
  errorMessage String? @map("error_message")
  summaryJson Json? @map("summary_json")
  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @default(now()) @map("updated_at")

  @@index([userId, createdAt(sort: Desc)], map: "idx_account_delete_requests_user_created")
  @@index([status, etaAt(sort: Asc)], map: "idx_account_delete_requests_status_eta")
  @@map("account_delete_requests")
}

model AuditLog {
  auditId      String   @id @map("audit_id") @db.VarChar(64)
  userId       String   @map("user_id") @db.VarChar(64)
  traceId      String   @map("trace_id") @db.VarChar(64)
  requestId    String   @map("request_id") @db.VarChar(64)
  ip           String   @db.VarChar(128)
  userAgent    String   @map("user_agent")
  action       String   @db.VarChar(128)
  resourceType String   @map("resource_type") @db.VarChar(32)
  resourceId   String?  @map("resource_id") @db.VarChar(64)
  metaJson     Json     @map("meta_json")
  createdAt    DateTime @default(now()) @map("created_at")

  @@index([userId, createdAt(sort: Desc)], map: "idx_audit_logs_user_created")
  @@index([action, createdAt(sort: Desc)], map: "idx_audit_logs_action_created")
  @@map("audit_logs")
}

model ComplianceIdempotency {
  id             String   @id @db.VarChar(64)
  userId         String   @map("user_id") @db.VarChar(64)
  idempotencyKey String   @map("idempotency_key") @db.VarChar(128)
  payloadHash    String   @map("payload_hash")
  resultJson     Json     @map("result_json")
  createdAt      DateTime @default(now()) @map("created_at")
  updatedAt      DateTime @default(now()) @map("updated_at")

  @@unique([userId, idempotencyKey])
  @@map("compliance_idempotency")
}
